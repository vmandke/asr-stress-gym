// Package dash serves the static dashboard HTML, the /api/events SSE
// stream, and the /api/chaos/{worker}/{action} control-plane endpoints.
// Deliberately boring: no build step, no framework. Filled in at M9,
// timeboxed to one day.
package dash
